# TODO: Code must be validated before usage, as some ukknown things till must be learned ;)
#install(TARGETS x_internal_blast DESTINATION bin)
# create configuration file from .in file

# install configuration file
#INSTALL( FILES "${PROJECT_BINARY_DIR}/${PROJECT_NAME}Config.cmake" DESTINATION . )

#
# Builds a CPack driven instalation package
#include(InstallRequiredSystemLibraries)
#set(CPACK_RESOURCE_FILE_LISENCE "${CMAKE_CURRENT_SOURCE_DIR}/Licence.txt")

#include(CPack)